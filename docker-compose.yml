version: '3.4'

services:
  pifancontrol:
    image: pifancontrol
    build:
      context: .
    dockerfile: ./Dockerfile
    environment:
      - LD_LIBRARY_PATH=/opt/vc/lib
    volumes:
      - /opt/vc:/opt/vc:ro
    devices:
      - /dev/vchiq
    privileged: true  
